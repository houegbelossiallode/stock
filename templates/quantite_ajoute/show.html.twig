{% extends 'base.html.twig' %}

{% block title %}QuantiteAjoute{% endblock %}

{% block body %}
    <h1>QuantiteAjoute</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quantite_ajoute.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ quantite_ajoute.quantite }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ quantite_ajoute.createdAt ? quantite_ajoute.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_quantite_ajoute_index') }}">back to list</a>

    <a href="{{ path('app_quantite_ajoute_edit', {'id': quantite_ajoute.id}) }}">edit</a>

    {{ include('quantite_ajoute/_delete_form.html.twig') }}
{% endblock %}
